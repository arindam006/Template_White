// Generated by CoffeeScript 1.3.3
var SecurityTab;

SecurityTab = {
  init: function() {
    var change_password_links;
    change_password_links = $j('.show-change-password');
    return change_password_links.on('click', function(e) {
      var role;
      role = $j(e.target).data('role');
      role && Multiaccount.is_valid_role(role) && PasswordChange.set_role(role);
      $('newpass').value = '';
      $('oldpass').value = '';
      PasswordChange.show_change_password_modal();
      return e.preventDefault();
    });
  }
};
